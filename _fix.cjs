const fs=require("fs");let c=fs.readFileSync("src/pages/admin/SummerCamp.tsx","utf-8");const o="const sessionDate = addWeeks(baseDate, w);";const n="const weekBase = addWeeks(baseDate, w);const dow = weekBase.getDay();const diff = cohort.weekDay - dow;const target = new Date(weekBase);target.setDate(target.getDate() + diff);const sessionDate = target;";if(c.includes(o)){c=c.replace(o,n);fs.writeFileSync("src/pages/admin/SummerCamp.tsx",c);console.log("OK")}else{console.log("NOT FOUND")}
